apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nanobot-data
  namespace: nanobot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
