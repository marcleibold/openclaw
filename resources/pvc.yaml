apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-agent-home-pvc
  namespace: ai-agent
  labels:
    app: ai-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
